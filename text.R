#This is a file to contain lengthy text for the UI

app_title<-"US Covid 19 Deaths Dashboard"

app_purpose<-"This will be a description of the purpose of the app"

about_data<-"Briefly discuss the data and its source - providing a link to more information about the data"

tab_purpose<-"Tell the user the purpose of each tab (page) of the app"

lm_info<-"You should explain these three modeling approaches,the benefits of each, and the drawbacks of each. You should include some type 
of math type in the explanation (you’ll need to include mathJax)."

tree_info<-"You should explain these three modeling approaches,the benefits of each, and the drawbacks of each. You should include some type of math type in the explanation (you’ll need to include mathJax)."

rf_info<-"You should explain these three modeling approaches,the benefits of each, and the drawbacks of each. You should include some type of math type in the explanation (you’ll need to include mathJax)."

model_options<-"Select your desired options below. When you have selected the options that you want for your models click on the button that says RUN ALL MODELS"

data_split<-"What percent of the dataset do you want to use to create your training data set? The remaining data will be used for the test data set"

tree_vars<-"Select all variables that you want to include in your regression tree model. If no variables are selected a regression tree model will be built using all variables."

rf_vars<-"Select all variables that you want to include in your random forest model. If no variables are selected a random forest model will be built using all variables."
